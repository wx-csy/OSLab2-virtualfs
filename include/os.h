#ifndef __OS_H__
#define __OS_H__

#include <kernel.h>

/* This function has been implemented in libc. So we no longer
 * need it here.
static inline void puts(const char *p) {
  for (; *p; p++) {
    _putc(*p);
  }
}
*/

#endif
